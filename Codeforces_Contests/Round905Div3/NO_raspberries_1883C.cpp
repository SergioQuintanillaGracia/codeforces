#include <iostream>
#include <vector>

using namespace std;

int main() {
    int cases;

    cin >> cases;

    for (int i = 0; i < cases; i++) {
        vector<int> nums;
        int n, k;

        cin >> n >> k;

        for (int j = 0; j < n; j++) {
            // Read the data and fill the nums vector
            int num;
            
            cin >> num;

            nums.push_back(num);
        }

        if (k == 2) {
            
        }

        nums.clear();
    }

    return 0;
}